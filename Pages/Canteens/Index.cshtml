@page
@model Jopp_lunch.Pages.Canteens.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Canteen[0].nazev)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Canteen) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nazev)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.cislo_VM">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.cislo_VM">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.cislo_VM">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
